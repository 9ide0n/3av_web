---
  locale: ru_RU.UTF-8
  project_python_version: 3
  project_postgres_version: 9.6

  project_app: djangopg
  project_name: "{{ project_app }}"
  project_home: "/projects/web1"

  project_logs: "{{ project_home }}/logs"

  project_repo_url: https://github.com/9ide0n/djangopg.git
  project_repo_path: "{{ project_home }}/{{ project_name }}"

  project_path: "{{project_repo_path}}/{{ project_name }}"
  project_user: "{{ ansible_user }}"
  project_venv_home: "{{ project_home }}/.virtualenvs"
  project_venv_path: "{{ project_venv_home }}/{{ project_name }}"
