project_provider: vagrant

project_vagrant_nodes:
  - name: db
    groups:
      - dbservers
      - queues
    ip: '192.168.200.20'
  - name: web
    groups:
      - webservers
      - taskservers
    ip: '192.168.200.10'
  - name: deb
    groups:
      - webservers
      - taskservers
    ip: '192.168.200.11'    
  - name: task1
    groups:
      - taskservers
    ip: '192.168.200.31'
  - name: task2
    groups:
      - taskservers
    ip: '192.168.200.32'

project_dbhost: "{{project_vagrant_nodes[0].ip}}"
project_queue_host: "{{project_vagrant_nodes[0].ip}}"
project_memcached_host: "{{project_vagrant_nodes[1].ip}}"
project_domains:
- "{{inventory_hostname}}"
- "{{inventory_hostname}}.dev.local"


#
